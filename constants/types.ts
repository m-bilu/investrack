export type SearchResultType = {
  exchange: string;
  name: string;
  sector: string;
  symbol: string;
};

export type NewsType = {
  headline: string;
  summary: string;
  image: string;
  url: string;
};
